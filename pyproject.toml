[project]
name = "ankagent"
version = "0.1.0"
description = "This is a self-improving agent"
readme = "README.md"
authors = [
    { name = "TinyMirable", email = "Marcille666@gmail.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
ankagent = "ankagent:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
